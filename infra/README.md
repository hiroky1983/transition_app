# terraform

set up gcloud

```bash
gcloud auth login
gcloud config set project [project_id]
```

check gcloud

```bash
gcloud auth list
gcloud config list
```

set up terraform

```bash
terraform init
terraform plan
terraform apply
```

destroy

```bash
terraform destroy
```
